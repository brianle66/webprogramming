document.addEventListener('DOMContentLoaded', function() {
    document.querySelector('#selectedStyle').addEventListener('submit', (event) => calculateproject(event))
});

function calculateproject(event){
    
}